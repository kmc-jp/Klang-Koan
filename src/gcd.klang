def gcd_iterative(int a, int b) -> (int) {
  while (b =/ 0) {
    def int r := a % b;
    a := b;
    b := r;
  }
  return a;
}

def gcd_recursive(int a, int b) -> (int) {
  if (b = 0) {
    return a;
  } else {
    return gcd_recursive(b, a % b);
  }
}
